import { View, StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
  safeArea: {
    flex: 0,
    backgroundColor: "#022D3B", // Light, trust-evoking blue
  },
  // other global styles...
});
